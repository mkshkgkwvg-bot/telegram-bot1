import logging
import instaloader
from telegram import Update
from telegram.ext import ApplicationBuilder, CommandHandler, ContextTypes

TOKEN="8667471311:AAGn0Mm0UElKpnTIWxzd2R9MawF0TZdTHy4"

logging.basicConfig(level=logging.INFO)

loader = instaloader.Instaloader()

async def start(update: Update, context: ContextTypes.DEFAULT_TYPE):
    await update.message.reply_text("ابعت /download + رابط انستجرام")

async def download(update: Update, context: ContextTypes.DEFAULT_TYPE):
    try:
        url = context.args[0]

        if "instagram.com" not in url:
            await update.message.reply_text("رابط غلط")
            return

        shortcode = url.split("/")[-2]
        post = instaloader.Post.from_shortcode(loader.context, shortcode)

        loader.download_post(post, target="downloads")

        await update.message.reply_text("تم التحميل")

    except Exception as e:
        await upapp = ApplicationBuilder().token("8667471311:AAGn0Mm0UElKpnTIWxzd2R9MawF0TZdTHy4" 4).build()

app.add_handler(CommandHandler("start", start))
app.add_handler(CommandHandler("download", download))

print("Bot running...")

app.run_polling()
